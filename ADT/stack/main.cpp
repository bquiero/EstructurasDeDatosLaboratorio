#include <iostream>
#include "ListStack.hpp"
//#include "ArrayStack.h"

int main(int argc, char const *argv[])
{
  /* g++ main.cpp ListStack.cpp -o program.out
  StackADT *ls = new ListStack();
  ls->push(4);
  ls->push(3);
  ls->pop();
  ls->top();
  */
 /* g++ main.cpp ArrayStack.cpp -o program.out
  StackADT *as = new ArrayStack();
  as->push(4);
  as->push(3);
  as->pop();
  as->top();
  */
  return 0;
}
